import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.createit.toolbox',
  appName: 'tool-box',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
